---
title: "SARC Research"
summary: "Historical, conceptual, technical, and artistic references surrounding SARC."
type: library
# This section index doubles as research's own Collection-summary Entry at
# the Library root (/library/) — see docs/library-v2.md § 11. id/type/color
# here duplicate data/library/collections.yaml's engine-authoritative
# registry entry deliberately: this is what lets the root reuse
# library-record.html/library-thumbnail.html/library-filters.html
# unchanged (they read real Page.Params, not registry data). Keep these
# three fields in sync with the registry entry of the same id —
# library-validate.html checks it.
library:
  id: research
  type: collection
  color: forest
# Placeholder cover — a plain square in this Collection's own Colorplan
# color (data/library/collections.yaml), generated by
# scripts/generate-library-collection-covers.py (`make library-covers`),
# not a photograph. Exists so this Collection-summary Entry has a primary
# image and isn't simply omitted from the Library root's Images view (the
# existing, correct behavior for any Entry with no image).
images:
  - file: "cover.png"
    alt: "Solid square in Colorplan Forest, representing the SARC Research collection"
    role: cover
    credit: "SARC"
    rights:
      status: sarc-owned
outputs:
  - HTML
  - JSON
draft: false
---
